apiVersion: v1
kind: Service
metadata:
  name: production-devops-service
spec:
  selector:
    app: production-devops
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
  type: LoadBalancer
